# Optional: full API URL (bypasses Vite proxy). Leave unset in dev to use /api/v1 proxy.
# VITE_API_BASE=http://127.0.0.1:8000/api/v1

# Where Vite proxies /api in dev (default: http://127.0.0.1:8000)
# VITE_PROXY_API=http://127.0.0.1:8000
